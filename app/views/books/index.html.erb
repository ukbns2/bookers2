<h2>New Book</h2>

<%= form_with model: @book do |f| %>
 <h4>Title</h4>
  <%= f.text_field :title %>
 <h4>Opinion</h4>
  <%= f.text_field :body %>

  <%= f.submit 'Create Book' %>
<% end %>

<table>
 <thead>
  <tr>
   <th></th>
   <th>Title</th>
   <th>Body</th>
 </thead>
 <% @books.each do |book| %>
  <tbody>
   <td></td>
   <td><%= book.title %></td>
   <td><%= book.body %></td>
  </tbody>
 <% end %>
</table>