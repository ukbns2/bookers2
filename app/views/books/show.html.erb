<p class ="notice"><%= flash[:notice] %></p>

<div class ="container px-5 px-sm-0">
 <div class ="row">
  <div class ="col-md-3">
   <%= render 'users/user_info', user: @book.user %>

    <h2 class ="mt-3">New book</h2>
    <%= form_with model: @books do |f| %>
     <div class ="form-group">
      <label for="book_title">Title</label>
       <div><%= f.text_field :title %></div>
     </div>
     <div class ="form-group">
      <label for ="book_opinion">Opinion</label>
       <div><%= f.text_area :body %></div>
     </div>
     <div class ="form-group">
       <div><%= f.submit 'Create Book', class: 'btn btn-success' %></div>
     </div>
    <% end %>
  </div>

  <div class ="col-md-8 offset-md-1">
   <h2>Book detail</h2>

   <table class ="table">
     <tbody>
      <tr>
       <td><%= link_to image_tag @book.user.get_profile_image("80x80") %><br><%= @book.user.name %></td>
       <td><%= @book.title %></td>
       <td><%= @book.body %></td>
       <% if @book.user == current_user %>
        <td><%= link_to "Edit", edit_book_path(@book), class: 'btn btn-sm btn-success' %></td>
        <td><%= link_to "Destroy", book_path(@book), method: :delete, class: 'btn btn-sm btn-danger', "data-confirm" => "Are you Sure？" %></td>
       <% end %>
     </tbody>
   </table>
  </div>
 </div>
</div>


